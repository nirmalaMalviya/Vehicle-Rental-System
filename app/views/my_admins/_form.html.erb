<%= form_for(my_admin) do |f| %>
  <% if my_admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(my_admin.errors.count, "error") %> prohibited this my_admin from being saved:</h2>

      <ul>
      <% my_admin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :age %>
    <%= f.number_field :age %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
